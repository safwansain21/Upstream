import type { Metadata } from "next";
import Link from "next/link";
import { AppHeader } from "../../components/app-header";
import { MotionSettings } from "../../components/motion-settings";
import { Footer, PolicyPage } from "../../components/ui";
export const metadata: Metadata = { title: "Accessibility" };
export default function AccessibilityPage() { return <><AppHeader/><PolicyPage title="A place for every contributor."><p>Upstream is designed for keyboard navigation, readable forms, visible focus, and use on small screens. Our accessibility target is WCAG 2.2 AA; this statement is a commitment to improvement, not a claim of independent certification.</p><MotionSettings/><h2>Navigate your way</h2><p>Use the skip link to move directly to the main content. All primary navigation and actions can be reached with a keyboard. On a smaller screen, the Menu button opens the primary navigation.</p><h2>Maps and evidence</h2><p>Network schematics provide selectable station lists. Reach states use labels and patterns as well as color. Essential evidence should remain available in text and tables, without requiring a map gesture.</p><h2>When something gets in the way</h2><p>If a page or workflow prevents you from contributing, share the page address, the action you were trying to complete, and any assistive technology you use with your organization’s coordinator. Avoid including private case details in a public report.</p><p>For a service interruption, check <Link href="/status">service status</Link>. You can learn about the <Link href="/how-it-works">investigation process</Link> without signing in.</p></PolicyPage><Footer/></>; }
